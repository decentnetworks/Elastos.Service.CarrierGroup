[chatrobot]
# Absolute path for runtime data (manager DB + per-group DBs).
data_dir = /home/<user>/devs/Elastos.Service.CarrierGroup/linux/ui/runtime_data

# Local manager socket bind address.
socket_ip = 127.0.0.1

# Local manager socket bind port.
socket_port = 2222
